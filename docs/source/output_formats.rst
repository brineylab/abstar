.. _output-formats:

output formats
==============

``abstar`` can output annotations in several different formats. The format is specified using the ``-f`` 
option, which can be used to specify the output format.